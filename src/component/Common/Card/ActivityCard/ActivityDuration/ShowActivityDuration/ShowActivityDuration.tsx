import React from "react";
import type IShowActivityDuration from "./IShowActivityDuration";

const ShowActivityDuration: React.FC<IShowActivityDuration> = ({ }) => {
    return (
        <section className="flex w-full flex-col text-sm text-gray-600">
            {/* Display activity duration here */}
        </section>
    );
};

export default ShowActivityDuration;